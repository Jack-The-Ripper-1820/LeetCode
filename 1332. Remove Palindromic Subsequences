class Solution {
public:
    int removePalindromeSub(string s) {
        if(s.empty())
            return 0;
        
        string sr = s;
        reverse(sr.begin(), sr.end());
        if(s == sr)
            return 1;
        
        return 2;
    }
};
